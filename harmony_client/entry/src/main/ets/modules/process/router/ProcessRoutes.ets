/**
 * 进程模块路由（对齐 Flutter AppProcessListPage：全屏进程列表页，无入参）。
 */

/** 进程模块页面名常量（主线程 AppRouter 接线用） */
export class ProcessRoutes {
  static readonly PROCESS_LIST: string = 'ProcessListPage';
}

/** 进程列表页参数（页面无入参，保留空类以对齐路由接线惯例） */
export class ProcessListPageParam {
}
