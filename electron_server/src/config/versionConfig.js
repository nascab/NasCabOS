/**
 * 服务端版本号（整数），用于登录等接口返回，便于客户端做版本判断
 */
const SERVER_VERSION = 10;
const UPDATE_FEED_URL = 'https://download.nas.cab/package/';
const HOME_PAGE_URL = 'https://nas.cab';
module.exports = {
  SERVER_VERSION,
  UPDATE_FEED_URL,
  HOME_PAGE_URL,
};
